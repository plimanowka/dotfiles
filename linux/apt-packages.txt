# APT packages for Debian/Ubuntu
# Install with: xargs -a apt-packages.txt sudo apt-get install -y

# Core tools
zsh
git
curl
wget
jq
tree
htop
fzf
zsh-syntax-highlighting
unzip
zip

# Python
python3
python3-pip

# Note: bat/ripgrep have different names on Ubuntu
# bat -> installed via install-tools.sh (batcat symlink or manual)
# ripgrep -> installed via install-tools.sh
